---
tags: [אינפי1, יחידה5, רציפות]
aliases: [משפט הפונקציה ההפוכה, arcsin, arccos, arctan, פונקציות הפוכות]
---

# פונקציות מונוטוניות והפונקציה ההפוכה

> מקור: כרך ב, סעיף 5.2.5 (משפטים 5.39–5.43, טענה 5.44, הגדרה 5.45)

## מונוטוניות וגבולות חד-צדדיים (משפטים 5.39–5.41)

- $f$ מונוטונית וחסומה ב-$(a,b)$ ⟹ הגבולות החד-צדדיים בקצוות קיימים וסופיים (5.39). ההוכחה: עבור עולה, $\lim_{x\to b^-}f(x)=\sup f((a,b))$ — קיים לפי [[חסמים - חסם עליון ותחתון|תכונת החסם העליון]], והאפיון של sup נותן את ההתכנסות. **בדיוק אותו רעיון כמו [[סדרות מונוטוניות|משפט 3.16 לסדרות]]** — מונוטוניות + חסימות = גבול, בעולם הפונקציות.
- לפונקציה עולה: בכל נקודה פנימית $\lim_{x\to x_0^-}f \le f(x_0) \le \lim_{x\to x_0^+}f$ (5.40) — שני החד-צדדיים תמיד קיימים, והערך כלוא ביניהם.
- לכן: **אי-רציפות של מונוטונית היא תמיד קפיצה** — [[מיון נקודות אי-רציפות|מין ראשון]] (משפט 5.41). מונוטונית לא יכולה להתנדנד ולא לברוח לאינסוף בנקודה פנימית.

## קריטריון רציפות למונוטונית (משפט 5.42)

$f$ מונוטונית בקטע $I$: אם התמונה $f(I)$ היא קטע ⟹ $f$ רציפה.

הרעיון: מונוטונית יכולה להיכשל רק בקפיצה (5.41), וקפיצה מנקבת "חור" בתמונה — הערכים שבין $\lim_{x\to x_0^-}f$ ל-$\lim_{x\to x_0^+}f$ (פרט אולי ל-$f(x_0)$) לא מתקבלים, אז התמונה אינה קטע. זהו **היפוך חלקי של [[משפט ערך הביניים]]** — עבור מונוטוניות, "תמונה = קטע" שקול לרציפות. (בלי מונוטוניות ההיפוך נכשל — $\sin\frac1x$.)

## משפט הפונקציה ההפוכה (5.43)

אם $f$ **מונוטונית ממש ורציפה** בקטע $I$, אז:
- התמונה $f(I)$ היא קטע (ערך הביניים);
- קיימת הפוכה $f^{-1}: f(I) \to I$ (מונוטונית ממש ⟹ חח"ע — [[פונקציות ממשיות - מושגי יסוד]]);
- $f^{-1}$ **מונוטונית ממש באותו כיוון ורציפה** ב-$f(I)$.

רציפות ההפוכה מוכחת אלגנטית דרך 5.42: $f^{-1}$ מונוטונית, ותמונתה היא $I$ — קטע — אז היא רציפה. (בלי לגעת ב-$\varepsilon$ אחד!)

**זה המשפט שמייצר את כל "הפונקציות ההפוכות" של הקורס** כפונקציות רציפות לגיטימיות: שורשים ($\sqrt[n]{x}$ כהפוכת $x^n$), הטריגו ההפוכות (להלן), והלוגריתם ([[פונקציות מעריכיות ולוגריתמיות|יחידה 6]]).

## הפונקציות הטריגונומטריות ההפוכות (טענה 5.44, הגדרה 5.45)

הטריגונומטריות אינן חח"ע (מחזוריות!) — [[פונקציות ממשיות - מושגי יסוד|מצמצמים]] לקטע שבו הן מונוטוניות ממש, ומפעילים את 5.43:

| פונקציה | קטע הצמצום | הפוכה | תחום ⟶ תמונה | הערות |
|---|---|---|---|---|
| $\sin$ | $[-\frac\pi2,\frac\pi2]$ עולה | $\arcsin$ | $[-1,1]\to[-\frac\pi2,\frac\pi2]$ | אי-זוגית |
| $\cos$ | $[0,\pi]$ יורדת | $\arccos$ | $[-1,1]\to[0,\pi]$ | $\arccos x=\frac\pi2-\arcsin x$ |
| $\tan$ | $(-\frac\pi2,\frac\pi2)$ עולה | $\arctan$ | $\mathbb{R}\to(-\frac\pi2,\frac\pi2)$ | אסימפטוטות אופקיות $\pm\frac\pi2$ |
| $\cot$ | $(0,\pi)$ יורדת | $\operatorname{arccot}$ | $\mathbb{R}\to(0,\pi)$ | |

כולן רציפות לפי משפט 5.43 — בלי שום עבודה נוספת.

### ⚠️ מלכודת ההרכבות

- $\sin(\arcsin x)=x$ לכל $x\in[-1,1]$ — תמיד נכון (הרכבה בכיוון "פנימה לתחום").
- $\arcsin(\sin x)=x$ **רק** עבור $x\in[-\frac\pi2,\frac\pi2]$! מחוץ לקטע: $\arcsin(\sin\pi)=\arcsin(0)=0\ne\pi$. הכלל: $\arcsin(\sin x)$ היא "קיפול" מחזורי של $x$ לקטע הצמצום. אותה זהירות ב-$\arccos(\cos x)$ ו-$\arctan(\tan x)$.

זהויות שימושיות: $\arcsin x+\arccos x=\frac\pi2$; $\ \cos(\arcsin x)=\sqrt{1-x^2}$ (ציור משולש ישר-זווית); $\ \arctan x+\arctan\frac1x=\frac\pi2$ ל-$x>0$.

## כרטיס דיוק

- **רעיון-על**: מונוטוניות נותנת סדר, חח"ע וגבולות חד-צדדיים; רציפות סוגרת את החורים בתמונה; יחד — הפוכה רציפה בחינם.
- **מתי מפעילים**: קיום ורציפות של הפוכה; מיון אי-רציפויות של מונוטונית; והגדרה נכונה של arc-ים עם תחומי הצמצום.
- **בדיקה עצמית**: (א) הוכיחו שמונוטונית עם תמונה-קטע רציפה (דרך 5.41). (ב) חשבו $\arcsin\left(\sin\frac{5\pi}{6}\right)$ (תשובה: $\frac\pi6$). (ג) למה $\arctan$ מוגדרת על כל $\mathbb{R}$ אבל $\arcsin$ רק על $[-1,1]$?
- **מלכודת**: מונוטונית אינה בהכרח רציפה (קפיצות מותרות); ו-$\arcsin(\sin x)=x$ רק בקטע הצמצום.
- **קישור רוחב**: ראו גם [[מפת תלות משפטים - אינפי 1]], [[תבניות הוכחה באינפי 1]], [[אינדקס דוגמאות נגד - אינפי 1]].

## קשרים

- [[פונקציות ממשיות - מושגי יסוד]] — חח"ע, צמצום והפוכה
- [[חסמים - חסם עליון ותחתון]], [[סדרות מונוטוניות]] — אותו רעיון sup בעולם הסדרות
- [[מיון נקודות אי-רציפות]] — מונוטונית: רק קפיצות
- [[משפט ערך הביניים]] — למה התמונה קטע
- [[פונקציות מעריכיות ולוגריתמיות]] — הלוגריתם כהפוכה ביחידה 6
- [[נגזרת הפונקציה ההפוכה]] — הנוסחה $(f^{-1})' = \frac{1}{f'\circ f^{-1}}$ ביחידה 7
- [[נגזרות הפונקציות האלמנטריות]] — נגזרות ה-arc-ים
